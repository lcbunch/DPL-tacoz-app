# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rake secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

development:
  secret_key_base: 9eafcd4eae09e56d3f0c3baa0596203f44482c05972328fc7e2f3c8c12afa38e4a8e164531fddd936ebef10fb1cf56767e114a44d80e1c4fdcef832cd2221d32
  twitter:
    consumer_key: <%= ENV['TWITTER_API_KEY'] %>
    consumer_secret: <%= ENV['TWITTER_API_SECRET'] %>
    access_token: <%= ENV['TWITTER_ACCESS_TOKEN'] %>
    access_token_secret: <%= ENV['TWITTER_ACCESS_TOKEN_SECRET'] %>

test:
  secret_key_base: 3db231f7084fd9f11e87591abca9ea1b2675cc5c83e7a0ac4a984c2dab225d4af7653571ed27ede593a9a631e87cb9b269322c6e6f50572bae71b139ad8854a0

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
